-- Canonical schema and seed data for the store (ms) database.
-- Applied automatically on first boot via
-- project/pg-init-scripts/10-init-databases.sh
-- See project/pg-init-scripts/sql/20-store-schema.sql for the authoritative copy.

-- Demo users (BCrypt hashed passwords):
--   klhomme0  / user1Pass!  (special product user)
--   ewhicher1 / user2Pass!
--   jdecreuze2 / user3Pass!  (special product user)
--   admin1    / adminPass!   (special product user)
